# v7 Audit Gap List

| Spec item | Current implementation | Evidence/path | Gap | Severity | Proposed fix |
| --- | --- | --- | --- | --- | --- |
| Admin password login uses signed HttpOnly cookies with expiry | Implemented with HMAC-signed admin token, `/admin` and `/api/admin` scoped cookies, 18 hour max age, production-only secure cookies | `src/lib/auth.ts`, `app/api/admin/login/route.ts` | No gap found | None | Preserve |
| `/admin/*` redirects and `/api/admin/*` returns 401 without admin session | Middleware enforces both; admin route handlers also call `requireAdminRequest` directly | `middleware.ts`, `src/lib/http.ts`, `tests/supabase-migration.test.ts` | No gap found | None | Preserve |
| Admin login throttling | Supabase RPC throttle exists with local in-memory fallback keyed by platform IP and server-issued attempt cookie | `supabase/migrations/003_admin_login_throttle.sql`, `app/api/admin/login/route.ts`, `tests/api-flow.test.ts` | Local fallback resets on process restart, acceptable for demo/dev only | P2 | Preserve |
| Anonymous participant session and QR join flow | Server route initializes/reuses session, sets HttpOnly participant cookie and join guard, profile endpoint normalizes nickname/role and avatar | `app/api/session/init/route.ts`, `app/api/session/profile/route.ts`, `src/lib/data.ts`, `src/lib/uploads.ts` | No P0 gap found | None | Preserve |
| Prediction writes are server-side, transactional, and retry-safe | Local store uses file lock and request idempotency; Supabase uses `place_prediction_tx` with row locks for session, participant, wallet, market, aggregate, and position, plus `prediction_actions.request_id` uniqueness | `src/lib/store.ts`, `src/lib/data.ts`, `app/api/markets/[id]/predict/route.ts`, `supabase/migrations/010_prediction_idempotency.sql`, `supabase/migrations/026_zero_switch_impact_guard.sql`, `tests/rules.test.ts`, `tests/api-flow.test.ts` | No P0 gap found | None | Preserve |
| Whale Guard fair launch, step-up, cooldown, market share, price impact | Implemented in `calculateAllowedStake`, `placePrediction`, and `place_prediction_tx`; tested for fair-launch and caps | `src/lib/constants.ts`, `src/lib/store.ts`, `supabase/migrations/002_transactional_core.sql`, `tests/rules.test.ts` | No P0 gap found | None | Preserve |
| People Signal primary, MegaBuck Signal secondary, agents separated | Aggregates count human people separately and expose human/agent/combined signals | `src/lib/store.ts`, `supabase/migrations/002_transactional_core.sql`, `components/stage-view.tsx`, `tests/rules.test.ts` | No P0 gap found | None | Preserve |
| Mollie test-mode checkout creation | Checkout route rejects live keys, requires production test key, uses server-side Mollie create with `Idempotency-Key`, amount/currency/test metadata | `app/api/payments/mollie/create-test-checkout/route.ts` | No P0 gap found | None | Preserve |
| Mollie MegaBuck issuing only after server-side verification | Status/webhook paths call `verifyAndCreditPurchase`, which fetches Mollie server-side and verifies id, amount, currency, metadata | `src/lib/payments.ts`, `app/api/payments/mollie/webhook/route.ts`, `app/api/payments/mollie/status/route.ts` | No P0 gap found | None | Preserve |
| Duplicate Mollie webhook cannot double-issue MegaBucks | Local logic checks existing ledger; SQL locks purchase and unique-indexes checkout ledger by purchase | `src/lib/store.ts`, `supabase/migrations/001_vota_wtf_v7.sql`, `supabase/migrations/002_transactional_core.sql`, `tests/rules.test.ts` | No P0 gap found | None | Preserve |
| Ledger/history consistency for starter MegaBucks, commits, checkout MegaBucks, void refunds, and resolution settlement | Ledger entries exist for starter MegaBucks, prediction commits, checkout MegaBucks, void refunds, and correct-resolution MegaBuck settlement; voiding refunds and clears signal | `src/lib/store.ts`, `supabase/migrations/004_transactional_admin_void.sql`, `supabase/migrations/011_resolution_credit_settlement.sql`, `tests/rules.test.ts` | The original v7 spec excluded MegaBuck payouts, but current product behavior requires correct predictions to settle internal MegaBucks back to wallets on resolution | P0 | Added `resolution_credit` ledger type, idempotent wallet crediting in local resolve, and Supabase `resolve_market_tx` settlement guarded by a partial unique index |
| Payment and ledger naming | The v7 spec names `purchases` and `ledger_entries`; the pasted prompt mentions `payment_events` and `wallet_ledger` generically | `vota_wtf_v7_nextjs_admin_spec.md`, `supabase/migrations/001_vota_wtf_v7.sql` | Introducing duplicate table names would fork the existing payment model | None | Preserve spec-aligned `purchases` / `ledger_entries`; use purchase ids, market ids, ledger ids, and timestamps as correlation evidence |
| Oracle Score leaderboard ranks reputation, not wallet balance | Score recompute uses correct prediction actions with early and contrarian multipliers; leaderboard sorts by oracle score, then efficiency | `src/lib/store.ts`, `supabase/migrations/004_transactional_admin_void.sql`, `supabase/migrations/008_public_leaderboard_projection.sql` | No P0 gap found | None | Preserve |
| Public state endpoint is compact and cache-controlled | Public endpoint reads scoped state and sets `Cache-Control: public, s-maxage=1, stale-while-revalidate=5` | `app/api/events/[slug]/public-state/route.ts`, `src/lib/data.ts` | No P0 gap found | None | Preserve |
| Admin audit logs for lifecycle/payment actions | Market create/update/open/lock/resolve/void/stage/payment/MCP token actions append audit logs | `src/lib/store.ts`, `src/lib/data.ts`, Supabase admin migrations | No P0 gap found | None | Preserve |
| Optional agents and MCP do not expose admin/payment surfaces | MCP supports market reads, wallet, allowed stake, scoped `place_prediction`, and budget request only | `app/mcp/route.ts`, `tests/realtime.test.ts`, `tests/api-flow.test.ts` | No P0 gap found | None | Preserve |
| GitHub Actions CI | No workflow existed | `.github/workflows/verify.yml` | Missing CI gate for install/lint/tests/build/readiness | P1 | Added `Verify` workflow running `npm install` and `npm run verify` |
| Environment example completeness | Core deployed env existed, but prompt-requested compatibility names were absent | `.env.example`, `README.md` | Missing `APP_URL`, `WEBHOOK_BASE_URL`, `MOLLIE_PROFILE_ID`, `MOLLIE_TESTMODE_ONLY` placeholders | P2 | Added placeholders while keeping app runtime on `NEXT_PUBLIC_BASE_URL` |
